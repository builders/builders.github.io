<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="author" content="{{ site.author.name }}" />
  <meta name="description" content="{{ site.description }}" />
  <meta name="keywords" content="{{ site.keywords }}" />
  <meta name="generator" content="{{ site.generator }}" />
  <title>{{ site.name }} | {{ site.company.vision | lower if page.url == "/" else title }}</title>
  <link rel="shortcut icon" href="{{ site.images.favicon }}" />
  <link rel="stylesheet" href="https://use.typekit.net/lio5uid.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.1/css/bulma.min.css" />
  <link rel="stylesheet" href="/css/main.css" />
  <script defer src="https://use.fontawesome.com/releases/v5.1.0/js/all.js"></script>
  {% include "components/social-meta.njk" %}
  {% include "components/json-ld.njk" %}
</head>
